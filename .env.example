# Secret used for signing JWTs and other security-related features; replace in production
OUTFITGURU_SECRET_KEY=change-me
# Backend database connection string; defaults to local SQLite file
OUTFITGURU_DATABASE_URL=sqlite:///./outfitguru.db
# Allowed origins for browser requests; use a JSON array of URLs
OUTFITGURU_CORS_ORIGINS=["http://localhost:5173"]
